コマンドライン

コンパイルするには？

コンパイルにはまず javac.exeを使用してコンパイルの作業を行う必要があります
要するにビルドという処理が必要

javac 名前.java
※ミスった場合
・プログラムで記述したclass名と同じでないと成功しません
・パス指定を行っているか？

今回はjavac.exeは省略して.javaファイルが出来たらjavac.exeが処理されるようにしています

javac.exeを実行すると.classファイルというのが完成します。


実行するには？

CLASSファイルというものが必要となります。
CLASSファイルを作成するには.javaファイルをコンパイルすると
自動的に作成されます。

java 名前　　　※（.java）は必要ない

これで実行されます